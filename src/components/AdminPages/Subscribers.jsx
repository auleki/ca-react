import React from 'react';
import { WrapAdmin } from '../StyledComponents';

const Subscribers = () => {
	return (
		<WrapAdmin>
			<h2>Admin Subscribers Page</h2>
		</WrapAdmin>
	);
};

export default Subscribers;
