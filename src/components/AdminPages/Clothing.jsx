import React, { useState } from 'react';
import { WrapAdmin } from '../StyledComponents';

const Clothing = () => {
	return (
		<WrapAdmin>
			<h2>CLOTHING shown here</h2>
		</WrapAdmin>
	);
};

export default Clothing;
